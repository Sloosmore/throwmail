# agent-email

CLI for managing temporary email inboxes for verification flows, reading emails, and extracting links/codes.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
