# agent-email/bin

Entry point for the `agent-email` CLI. Contains a single file (`agent-email.js`) that imports the compiled `dist/index.js` and is registered as the `bin` executable in `package.json`.

> E2E verification requirements live in the flow graph — see flow skills via `npx agent-core search "flow/" --type skill`
